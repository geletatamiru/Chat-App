const ChatInput = () => {
  return (
    <div className="chat-input">
      <input type="text" placeholder="Send a message" />
      <button>Send</button>
    </div>
  );
};

export default ChatInput;
